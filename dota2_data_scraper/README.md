# Dota 2 Data Scraper

Автоматизированный скрапер для сбора данных о героях и фасетах Dota 2 с сайта dota2protracker.com

## 🚀 Возможности

- **Сбор данных о героях** - автоматический сбор статистики героев по позициям
- **Сбор данных о фасетах** - сбор информации о фасетах героев
- **Автоматический выбор периода** - сбор данных за последние 8 дней
- **Обработка диалоговых окон** - автоматическое закрытие рекламы и cookie consent
- **Создание конфигураций** - генерация JSON конфигураций для использования в игре
- **Логирование** - подробные логи всех операций

## 📁 Структура проекта

```
dota2_data_scraper/
├── modules/
│   ├── core/                    # Основные компоненты
│   │   ├── scraping_manager.py  # Менеджер скрапинга
│   │   ├── data_manager.py      # Менеджер данных
│   │   └── config_processor.py  # Процессор конфигураций
│   ├── scrapers/                # Скраперы
│   │   ├── hero_scraper.py      # Скрапер героев
│   │   └── facet_scraper.py     # Скрапер фасетов
│   ├── utils/                   # Утилиты
│   │   ├── period_selector.py   # Выбор периода
│   │   └── dialog_handler.py    # Обработка диалогов
│   └── config/                  # Конфигурации
│       └── hero_config.py       # Конфигурации героев
├── main.py                      # Основной скрипт
├── requirements.txt             # Зависимости
└── README.md                    # Документация
```

## 🛠️ Установка

1. **Клонирование репозитория:**

```bash
git clone <repository-url>
cd dota2_data_scraper
```

2. **Создание виртуального окружения:**

```bash
python -m venv env
```

3. **Активация виртуального окружения:**

```bash
# Windows
env\Scripts\activate

# Linux/Mac
source env/bin/activate
```

4. **Установка зависимостей:**

```bash
pip install -r requirements.txt
```

## 🚀 Использование

### Запуск всех процессов

```bash
python main.py
```

### Запуск отдельных компонентов

```bash
# Только скрапинг героев
python main.py --heroes

# Только скрапинг фасетов
python main.py --facets

# Только обработка конфигураций
python main.py --config

# Все процессы
python main.py --all
```

## 📊 Выходные файлы

После выполнения скрипта создаются следующие файлы:

- `heroes_data.csv` - данные о героях
- `facets_data.csv` - данные о фасетах
- `processed_heroes.csv` - обработанные данные героев
- `processed_facets.csv` - обработанные данные фасетов
- `hero_configs.json` - конфигурации для использования в игре

## 🔧 Технические особенности

### Автоматический выбор периода

Скрапер автоматически выбирает период "8 days" для получения актуальных данных.

### Обработка диалоговых окон

Автоматическое закрытие рекламных блоков и cookie consent для бесперебойной работы.

### Устойчивость к изменениям

Код адаптирован к изменениям верстки сайта и использует гибкие селекторы.

### Логирование

Подробное логирование всех операций для отладки и мониторинга.

## 🐛 Устранение неполадок

### Проблемы с Chrome

Если возникают проблемы с Chrome WebDriver:

1. Убедитесь, что Chrome установлен
2. Скрипт автоматически скачает нужную версию chromedriver
3. Проверьте, что нет других запущенных процессов Chrome

### Проблемы с сетью

При проблемах с доступом к сайту:

1. Проверьте интернет-соединение
2. Убедитесь, что сайт dota2protracker.com доступен
3. Попробуйте запустить скрипт позже

## 📝 Логи

Все операции логируются в консоль с временными метками. Уровни логирования:

- INFO - информационные сообщения
- WARNING - предупреждения
- ERROR - ошибки

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории с подробным описанием проблемы.
